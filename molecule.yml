---
ansible:
  config_file: ./ansible.cfg
  playbook: ./playbooks/site.yml
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-repertory
      image: ubuntu
      image_version: 16.04
      ansible_groups:
        - molecule
verifier:
  name: testinfra
